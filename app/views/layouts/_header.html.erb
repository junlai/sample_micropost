<header>
      <%= link_to logo, root_path %>
      <nav class="round">
        <ul>
          <li><%= link_to "主页", root_path %></li>
          <% if signed_in? %>
            <li><%= link_to "个人设置", current_user %></li>
          <% end %>
          <li><%= link_to "帮助",  help_path %></li>
          <% if signed_in? %>
             <li><%= link_to "退出", signout_path, :method => :delete %></li>
          <% else %>
             <li><%= link_to "登录", signin_path %></li>
          <% end %>        
        </ul>
      </nav>
</header>
